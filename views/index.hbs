
{{!-- <section>
    <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      <th scope="col">Mob</th>
    </tr>
  </thead>
  <tbody>
    {{#each users}}
    <tr>
      <th scope="row">1</th>
      <td>{{this.name}}</td>
      <td>{{this.email}}</td>
      <td>{{this.mob}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
</section> --}}

<div class="text-center center-div mt-5" id="login">
    <div class="container w-40  py-5 mt-5">
        <h1>Welcome to My Home Page</h1>
        <h3>Nice to Meet you {{#if user}}
             {{user.name}}
             {{else}}
              ! 
          {{/if}}</h3>
       <!-- <a href="/logout" class="btn btn-danger rounded-pill" style="position: absolute;top: 28px; right: 47px; padding: 0 26px; text-align: center;">LogOut</a>
       --></div>
</div>